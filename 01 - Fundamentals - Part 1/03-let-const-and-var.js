/* assignment 3
- set the value of 'language' to the language spoken where you live;
- think about which variables should be const variables (which values will never change and which might change) then change these variables to const.
- try to change one of the const variables now and observe what happens. */

// language = "Portuguese"; - commenting not to have an error
const country = "Brazil";
const continent = "South America";
// population remains 'let' because it can change
const isIsland = false;
const language = "Portuguese";