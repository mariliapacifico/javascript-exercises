/* assignment 2
- declare a variable called 'isIsland' and set value according to your country - the variable should hold a boolean value; 
- also declare a variable 'language', but don't assign any value yet.
- log their values to the console. */

let isIsland = false; // boolean
let language; // undefined

console.log(typeof isIsland);
console.log(typeof population);
console.log(typeof country);
console.log(typeof language);